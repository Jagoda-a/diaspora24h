name: Ingest every 45 minutes

on:
  schedule:
    - cron: "0,45 * * * *"   # na svaku punu uru i u :45 (UTC)
  workflow_dispatch:          # ručno pokretanje iz Actions taba

jobs:
  call-ingest:
    runs-on: ubuntu-latest
    steps:
      - name: Hit cron proxy (production)
        run: |
          curl -sS -X GET "https://diaspora24h.com/api/cron/ingest" \
            -H "User-Agent: diaspora24h-cron/1.0"
